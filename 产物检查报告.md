# 工作空间产物检查报告

> 检查对象：`F:\网站开发` 智能定投计算器全栈项目（smart-dca-calculator v2.0.0）
> 检查时间：2026-07-26
> 结论一句话：**核心产物完整、代码健康度良好（类型检查 0 错、11 测试全过），但有构建残留与两处历史遗留目录需清理，且核心计算内核缺少单元测试。**

---

## 一、项目概览

| 项 | 内容 |
|----|------|
| 名称 | 个人智能定投计算器 · 多策略版 |
| 技术栈 | React 18 + TypeScript + Ant Design 5（前端）｜ Vercel Serverless Functions + Neon Postgres（后端） |
| 当前版本 | 2.0.0（已支持注册/登录 + 多设备数据同步） |
| Git 状态 | working tree clean，最新提交 `146c2ed`，基于 origin/main（upstream 已失效，需 `git branch --unset-upstream`） |
| 部署目标 | Vercel（前端 + api/ 函数同域名）；腾讯云 SCF 方案已废弃 |

---

## 二、产物清单（按目录）

### 1. 前端源码 `src/`（28 文件）— ✅ 完整
- **计算内核 `lib/calc.ts`**：6 大策略纯函数，边界处理完善（`capMax` 上限封顶、`round2` 精度、`clamp`）。
  - 位置权重、估值百分位、阶梯档位、价值平均（VA，账户级）、网格、恒定比例再平衡 + 策略介绍页。
- **状态/同步**：`hooks/useAuth`、`hooks/useCloudSync`、`lib/syncSlice`、`lib/auth`、`components/AuthModal`、`MigrationDialog`、`SyncStatus`。
  - 同步设计清晰：仅同步「计算参数」（assets/预算/各策略 tier），UI 状态（activeStrategy/theme）留本地。
- **自动填充**：`lib/indicator.ts` 调后端 `/api/indicator` 取价格/30月均线/估值分位。
- **持久化**：`lib/storage.ts` localStorage（游客 + 已登录镜像缓存）。
- **主题**：`theme.ts` 金融蓝主色 + 明暗双主题，红涨绿跌在状态色固定（中国习惯）。
- 默认示例数据 `defaults.ts`：纳指100 / 中证500 / 黄金ETF 三标的。

### 2. 后端 `api/`（9 文件）— ✅ 完整
- `indicator.js`：GET `/api/indicator?code=&type=` 行情入口（CORS 双保险）。
- `lib/fetchData.js`：东财基金 NAV 全量 + 新浪日线，算 **30 月均线（真实近 30 月均值）** 与 **估值分位（10 年月度序列排名）**。
- 认证 `auth/*.js`：register/login/logout/me，bcrypt(10 轮) + JWT（httpOnly + sameSite=lax + 生产 secure）。
- `data.js`：GET/PUT `/api/data` 云端同步（JSONB，200 KB 上限防滥用）。
- `lib/db.js`：Neon Postgres 连接 + `users`/`user_data` 表，幂等建表。

### 3. 部署配置 — ✅ 到位
- `vercel.json`：函数超时 30s + 边缘 CORS。
- `vite.config.ts`：`base:'./'`、`build→dist/`、`emptyOutDir:false`（⚠️ 见第四节）。
- `DEPLOY-VERCEL.md` / `DEPLOY-AUTH.md`：部署与账号同步指引完整。

### 4. 构建产物 `dist/`（6 文件，已被 gitignore）— ⚠️ 有残留
- `index.html` 引用最新一套：`index-DOKpf0z2.js` + `index-CKw0bhQD.css`。
- 旧构建死文件（因 `emptyOutDir:false` 累积）：`index-DnISpp91.css`、`index-DrfS1dI9.js`、`index-CD35dlML.js`。

### 5. 被 gitignore 的历史遗留 — ⚠️ 建议清理
- `server/`：腾讯云 SCF 旧后端（已用 `api/` 替代，文档亦建议可删）。
- `dca-30mma-website/`：另一个无关项目的残留（仅含 `.verify_db/accounts.db` 一个 sqlite 账密库）。

---

## 三、健康度验证（已实地运行）

| 检查项 | 命令 | 结果 |
|--------|------|------|
| TypeScript 类型检查 | `npm run typecheck` | ✅ 0 错误 |
| 单元测试 | `npm run test`（vitest） | ✅ 11/11 通过（syncSlice 4 + auth 7） |

安全实践抽查：bcrypt 10 轮、JWT httpOnly+SameSite=Lax、CORS 边缘+函数双保险、payload 体积限制——均符合规范。

---

## 四、发现的问题与建议（按优先级）

### 🔴 P1：核心计算内核缺少单元测试
`lib/calc.ts` 是整套产品的金融逻辑核心（定投金额分配、上限截断、止盈判定），但**当前测试未覆盖**——只有 auth 与 syncSlice 的 11 个测试。建议补 `calc.test.ts`，至少覆盖：
- 位置权重合计=预算、命中单期上限时合计<预算；
- 估值百分位「极度高估（>high）」正确止盈暂停；
- 再平衡比例合计≠100% 的 warning 与分配逻辑；
- 阶梯/网格偏离比例边界与方向判定。

### 🟡 P2：dist/ 构建产物残留
`vite.config.ts` 设了 `emptyOutDir:false`，每次 build 不清空 dist，已累积两套 hash 文件（3 js + 2 css），其中 index.html 只引用最新一套，其余为死文件。虽 git 已忽略 dist，但本地磁盘冗余、易误用旧包。**建议**：改回 `emptyOutDir:true`（若无需保留历史包），或在 CI/部署前加 `rm -rf dist` 步骤。

### 🟡 P3：历史遗留目录易致混淆
`server/`（废弃 SCF 后端）与 `dca-30mma-website/`（无关项目）被 gitignore 但仍在磁盘。新人接手易误判。`dca-30mma-website/.verify_db/accounts.db` 还存着账密库，建议确认无价值后删除，避免泄露风险。

### 🟢 P4：注释与现状不符（小瑕疵）
`src/config.ts` 注释仍写「腾讯云 SCF API 网关地址」，实际已迁移 Vercel 同源部署；建议更新注释避免误导。

### 🟢 P5：估值口径对外需明示
`lib/fetchData.js` 用的是「价格/净值分位（10 年月度排名）」，**非官方 PE/PB 估值分位**；前端已标 `approx/参考`。用户若在页面外引用结论，需明确这是「参考分位」而非官方估值，以免误解。

### 🟢 P6：本地 dev 自动填充需配 env
本地 `npm run dev` 未注入 `VITE_API_BASE` 时，自动填充不启用（搜索下拉仍可用，但价格/均线/分位需手填）。本地联调需 `.env.local` 或 `vercel dev`。

---

## 五、总体结论

- **成熟度**：产品级。6 策略 + 账户系统 + 多设备同步 + 自动填充完整闭环，类型/测试通过，安全实践到位。
- **待办（按优先级）**：① 补 `calc.test.ts` 单元测试；② 清理 dist 残留与遗留目录；③ 更新过时注释；④ 对外明示估值口径。
- **可直接上线前提**：Vercel 注入 `VITE_API_BASE`、`POSTGRES_URL`(Neon)、`JWT_SECRET`、`INVITE_CODE` 四个环境变量后，`vercel --prod` 即可生产部署。
