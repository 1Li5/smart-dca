# 后端地址（Vercel Serverless Functions 部署后的域名，只到域名根，不含 /api/indicator）
# 留空 = 不启用自动填充（基金搜索下拉仍可用，只是选定后不自动拉价格/均线/估值）
# 部署到 Vercel 后，把下面这行改成你的 Vercel 域名，然后重新 vercel --prod
# 例：VITE_API_BASE=https://smart-dca-calculator.vercel.app
VITE_API_BASE=
