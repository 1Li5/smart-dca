# 前端 VITE_API_BASE = 后端 Serverless Functions 部署后的域名（Vercel 同源）
# 留空 = 不启用自动填充（基金搜索下拉仍可用）
# 部署到 Vercel 后，把下面改成你的 Vercel 域名
# 例：VITE_API_BASE=https://smart-dca-calculator.vercel.app
VITE_API_BASE=https://smart-dca-delta.vercel.app

# ---- 后端环境变量（Vercel 部署时设，不是 .env 文件）----
# 1. POSTGRES_URL  →  Vercel Storage 开通 Neon Postgres 后自动注入，不用手动设
# 2. JWT_SECRET    →  32+ 位随机串，用于签发登录态 token。设：openssl rand -base64 32
# 3. INVITE_CODE   →  你自定的注册邀请码（如 dctou2026），私发给家人朋友
