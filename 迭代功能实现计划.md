# 智能定投计算器 · 上线后迭代功能实现计划（规划版）

> 范围：**只规划、不写代码**。基于现状梳理（已读 `App.tsx` / `lib/calc.ts` / `lib/defaults.ts` / `components/StrategyView.tsx` / `ResultTable.tsx` / `IntroPage.tsx` / `theme.ts` / `config.ts` / `package.json` / `api/health.js` / `api/data.js` / `api/lib/db.js` / `index.css`）。
>
> 现状要点：
> - 计算内核 `calc.ts` 为 **6 策略纯函数 + `runStrategy` 分发**，输出 `StrategyResult { columns, rows, summary, warnings, copyLines }`；**核心计算逻辑不可轻易改动**（权限给主理人/风险复核）。
> - 策略结果已结构化（`result.rows` 含每标的金额/权重/偏离/占比等），可视化可直接派生，无需改 `calc`。
> - 后端 Vercel Serverless + Neon；`/api/health` 仅查 env 配置、不查真实连通；`db.js` 已封装 `getSql()`（neon）。
> - 主题 `theme.ts` 固定「红涨绿跌」（`StatusBadge` 用 `#cf1322` 涨 / `#389e0d` 跌），图表须沿用。
> - 依赖现状：AntD5 + icons 已引入；**图表库（@ant-design/charts / @ant-design/plots）、导出库（html2canvas / jspdf）、压缩（lz-string）均未引入**。
> - 打包：Vite，`chunkSizeWarningLimit:1500`；重依赖一律「动态 import」控 bundle。

---

## 方向一：数据可视化增强

### 1.1 实现方案（落到组件/交互）
- **本地图表（无需后端，纯前端派生，优先做）**
  - 本期定投金额分布：柱状图 / 饼图，数据源 `result.rows[*].amount`（asset 类策略）。
  - 持仓当前市值占比：饼图，数据源 `state.assets[*].currentValue`（rebalance / 通用）。
  - 估值 / 位置信号条：条形图，数据源 `result.rows` 的 `percentile` / `weight` / `dev`，按状态色区分低估/高估/合理。
- **回测图表（需历史月线，较重，放方向三协同）**
  - 回测净值曲线（折线，红涨绿跌）。
  - 组合回测对比（多策略净值叠加）。
- 图表统一挂在「计算结果」卡片下方，新增 `ResultCharts` 组件，按当前策略选择要画的图。

### 1.2 图表库选型（关键决策）
| 方案 | 体积(gzip) | 红涨绿跌/主题 | tree-shaking | 结论 |
|---|---|---|---|---|
| 轻量自绘 SVG 组件 | 0（自写） | 完全可控，直接复用 theme 色 | 最优 | **首选（MVP）** |
| @ant-design/plots | ~150KB+ | 需手动配色，G2 主题耦合 | 一般 | 仅当交互复杂（缩放/tooltip 成本高）时升级 |
| recharts | ~100KB | 易配色 | 较好 | SVG 成本超预期时的备选 |

> 推荐：**MVP 用自绘 SVG**（LineChart / BarChart / PieChart 三件套，各约 60–120 行），零依赖、最小 bundle、主题可控；回测曲线若交互过重再引 `recharts`（动态 import）。

### 1.3 涉及文件
- 新建 `src/components/charts/LineChart.tsx`、`BarChart.tsx`、`PieChart.tsx`（或合入 `charts/index.tsx`）。
- 新建 `src/components/ResultCharts.tsx`（按策略组装图表，宽 100% 响应式）。
- 修改 `src/components/StrategyView.tsx`：在 `ResultTable` 下挂 `<ResultCharts result={result} state={state} />`。
- 修改 `src/theme.ts`：导出图表色板（涨 `#cf1322` / 跌 `#389e0d` / 主色 `#1668dc` / 轴灰），供 SVG 取色。
- （回测部分见方向三 3.3）

### 1.4 新增依赖
无（MVP）。若后续引入：`recharts@^2`（动态 import，不进主包）。

### 1.5 工作量
本地图表 **M**（0.5–1 天）；含回测 **L**（>1 天，见方向三）。

### 1.6 优先级 / 分批
本地图表优先（**P1**，独立可提交）；回测并入方向三批次。

---

## 方向二：移动端响应式适配

### 2.1 实现方案
- 以 **AntD 5 响应式（Grid/Row/Col 断点 + `Grid.useBreakpoint()`）为主**，减少手写媒体查询（现状 `index.css` 仅 720px 一处，保留作兜底，新布局用断点）。
- 新建 `src/hooks/useResponsive.ts`：封装 `useBreakpoint()` → 暴露 `isMobile / isTablet` 便于复用。
- **AssetTable 移动化（最大改动）**：窄屏（xs）由可编辑 Table 切换为「每张卡片 = 一个标的」的 `Descriptions`/自定义卡布局（标签+输入框纵向堆叠），避免横向滚动。
- **GlobalSettings**：字段包进 `Row/Col`，`xs=24 sm=12 lg=8`，替换现有 `minWidth` 固定宽。
- **AppHeader**：移动端把「复制/重置/主题」收到 `Dropdown`「更多」或用图标按钮，避免换行拥挤。
- **ResultTable**：保留横向滚动，移动端字号略缩；可加「卡片视图」开关（可选）。
- 图表 / 分享卡在方向一、四已按 100% 宽自适应。

### 2.2 涉及文件
- 修改 `src/components/AssetTable.tsx`（响应式卡片视图，最大块）。
- 修改 `src/components/GlobalSettings.tsx`（Row/Col 包裹）。
- 新建 `src/hooks/useResponsive.ts`。
- 修改 `src/components/AppHeader.tsx`（紧凑 actions）。
- 修改 `src/components/StrategyView.tsx`（卡片区按断点微调间距）。
- 修改 `src/index.css`（保留/精简 720px 兜底，新增少量断点类）。
- 修改 `src/components/ResultTable.tsx`（移动字号）。

### 2.3 新增依赖
无（AntD Grid 已含）。

### 2.4 工作量
**M**（0.5–1 天，主要是布局重构，无计算逻辑改动，风险低）。

### 2.5 优先级 / 分批
建议 **最先做（P0）**，独立 commit；后续方向一/四的 UI 直接继承响应式外壳。

---

## 方向三：策略扩展

### 3.1 实现方案（用户点名的增强）
- **3a 定投止盈目标线**：`AppState` 增 `takeProfit` 配置（percentile 用「目标百分位」、grid/position 用「目标价格」）。`calc.ts` **仅新增分支**（参数未设时默认关闭 → 行为完全不变）：命中止盈线时 `warnings` 提示 + `rows` 加 `hitTakeProfit` 标记；图表上画一条止盈线（复用方向一 SVG）。
- **3b 动态再平衡频率**：`rebalance` 增 `frequency`（`monthly`|`quarterly`|`yearly`|`threshold`）与可选 `thresholdPct`。`calc.ts` 读取频率（默认 `monthly` 保持现状）；`GlobalSettings` 增频率选择器；回测时频率驱动再平衡事件。
- **3c 组合层面回测对比**（与方向一协同）：`BacktestPanel` 支持勾选多策略，叠加各自净值曲线 + 指标排名表（年化 / 最大回撤 / 期末市值）。（区别于现有 `ComparePanel`——后者只比「当期金额」，非历史绩效。）

### 3.2 涉及文件
- 修改 `src/lib/calc.ts`：**仅扩展类型与新增分支**（不动 6 个既有函数逻辑）；`AppState` 增字段，`percentile`/`grid`/`rebalance` 增可选分支。
- 修改 `src/lib/defaults.ts`：`DEFAULT_STATE` 补新字段（安全默认值，保证存量行为不变）；`STRATEGIES` 的 `assetFields` 视情况补充。
- 修改 `src/components/GlobalSettings.tsx`：percentile/grid 加止盈线输入；rebalance 加频率选择。
- 新建 `src/lib/backtest.ts`：纯函数，输入（策略 + 历史月线 + 参数）→ 净值序列 + 指标（与 `calc.ts` 解耦，新增模块）。
- 新建/修改 `src/components/BacktestPanel.tsx`：回测面板 + 多策略叠加（方向一图表组件复用）。
- 后端：扩展 `api/lib/fetchData.js` 或新建 `api/series.js` 暴露「历史月线」供回测（**需与行情数据工程师确认数据源/口径**）。
- 测试：补 `src/lib/calc.test.ts`（既有内核用例 + 3a/3b 新分支）。

### 3.3 新增依赖
无（纯逻辑 + UI；历史月线复用后端）。

### 3.4 工作量
3a **M**；3b **S–M**；3c（回测）**L**（含后端序列）。合计 **L**。

### 3.5 优先级 / 分批
3a/3b 为 **P1**（增强核心）；3c 为 **P2**（最重，依赖方向一图表 + 后端历史数据）。**必须与风险合规确认「止盈」措辞（不得暗示保本/收益承诺）**。

---

## 方向四：导出与分享

### 4.1 实现方案
- **一键复制配置**：扩展现有 `onCopy`，新增「复制为可导入 JSON / 文本」+「导入」粘贴还原（复用 `applySyncPayload` / `extractSyncPayload`）。
- **分享链接**：当前 `AppState` 经 `lz-string` 压缩后写入 URL hash（`#cfg=...`）；打开链接自动识别并还原。超长时降级为后端短链（见下）。
- **导出图片**：构建专用 `ShareCard`（纯内联样式、无 AntD token，规避 html2canvas 对 oklch/color-mix 的兼容问题），`html2canvas` 截图下载 PNG。
- **导出 PDF**：`html2canvas` 截 `ShareCard` → 塞入 `jspdf` 生成 PDF。
- **导出入口**：`AppHeader` 原「复制方案」按钮升级为 `Dropdown` 导出菜单（复制 / 分享链接 / 图片 / PDF / 导入）。

### 4.2 涉及文件
- 新建 `src/lib/share.ts`（state ⇄ URL hash，lz-string 编解码 + 容错）。
- 新建 `src/components/ShareCard.tsx`（自包含分享卡，内联样式，含品牌 + 策略名 + 参数 + 结果表）。
- 新建 `src/lib/exportImage.ts`、`exportPdf.ts`（**动态 import** html2canvas/jspdf，点按时才加载）。
- 新建 `src/components/ExportMenu.tsx`（Dropdown 菜单）。
- 修改 `src/components/AppHeader.tsx`（挂 `ExportMenu`，替换单一复制按钮）。
- 修改 `src/App.tsx`（挂载时读 `location.hash` 分享配置并询问还原）。
- 测试：`src/lib/share.test.ts`（编解码 round-trip）。
- （可选后端短链）新建 `api/share.js` + `share_links` 表（仅当 URL hash 超长时）。

### 4.3 新增依赖
- `html2canvas@^1.4.1`（~50KB gzip）—— 仅动态 import，不进主包。
- `jspdf@^2.5.2`（~70KB gzip）—— 仅动态 import。
- `lz-string@^1.5.0`（~3KB）—— 进主包无碍。
- 不引 `file-saver`（用 Blob + `a` 标签下载即可）。

> 风险：html2canvas 对 AntD5 新版色彩函数兼容差 → 用独立 `ShareCard` 内联样式规避；**务必动态 import 控 bundle**。

### 4.4 工作量
**M**（复制/导入 S，分享链接 M，图片/PDF M；后端短链额外 M，可选）。

### 4.5 优先级 / 分批
**P0**（用户价值高、相对独立）。与方向二共用 `AppHeader`，建议错开提交或同批协调。

---

## db-ping 真实连通探针（独立子项）

### 是否需要
**建议加**。现状 `/api/health` 的 `postgres` 字段只判断 `process.env.POSTGRES_URL` 是否存在（配置探针），不验证真实连通。监控告警（风险合规 task#2）需要「DB 真连不上」的信号，而非「配置在不在」。

### 怎么加
- 新建 `api/db-ping.js`：`GET /api/db-ping`，复用 `api/lib/db.js` 的 `getSql()`，执行 `SELECT 1`，计时返回 `{ status:'ok', ok:true, latencyMs }`；失败返回 `{ ok:false, error }`（**不抛 500**，避免监控误判「服务挂」）。`POSTGRES_URL` 缺失时返回 `ok:false, reason:'config missing'`。
- 保持 `/api/health` 轻量不变；db-ping 独立路由，监控改为/同时轮询 `/api/db-ping`。
- 无新依赖（复用 `@neondatabase/serverless`）。
- 工作量 **S**（<0.5 天），可随时独立 commit。建议与风险合规对齐监控接入。

---

## 总体排期建议（分批、每批独立 commit）

| 批次 | 内容 | 方向 | 工作量 | 依赖 | 优先级 |
|---|---|---|---|---|---|
| B0 | 补 `calc.test.ts` 内核用例（解锁后续 calc 改动） | — | S | 无 | P1 |
| A | 移动端响应式适配 | 二 | M | 无 | **P0** |
| B | 结果本地图表（SVG 三件套 + ResultCharts） | 一(本地) | M | 无 | P1 |
| C | 导出与分享（复制/导入 + 分享链接 + 图片/PDF 动态 import） | 四 | M | A(header) | **P0** |
| D | 止盈目标线 + 动态再平衡频率 + calc 测试 | 三(3a/3b) | M | B0 | P1 |
| E | 回测净值曲线 + 组合回测对比（后端序列 + backtest.ts + BacktestPanel） | 一(回测)+三(3c) | L | B,D,后端历史 | P2 |
| F | /api/db-ping 探针 | 监控 | S | 无 | 随时 |

### 建议顺序与理由
1. **先做 A（移动端）与 C（导出分享）**：风险低、用户感知强、互相独立（仅 `AppHeader` 共用，错开即可），快速出成果。
2. **B（本地图表）** 紧随：纯前端、无后端，复用现有 `result`，快速提升直观度。
3. **D（策略增强 3a/3b）**：在 B0 测试护航下扩展 `calc.ts`（仅新增），改动克制。
4. **E（回测）最后**：工作量最大、依赖后端历史月线（需与行情数据工程师确认 `/api/series` 口径）与 B/D，放收尾。
5. **F（db-ping）** 随时插入，配合风险合规的监控。

### 跨方向 / 跨角色协同提醒
- **风险合规**：止盈线措辞、分享卡/导出物须保留风险提示；db-ping 接入其监控告警。
- **行情数据工程师**：回测所需「历史月线」数据源与口径（复用 `fetchData.js` 的 10 年月线或新增序列接口）。
- **calc.ts 纪律**：所有扩展仅「新增字段/分支 + 安全默认」，6 个既有函数逻辑零改动；每次改动配对应单测。
